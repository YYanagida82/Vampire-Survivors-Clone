name: Deploy WebGL to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write # <-- Pagesへの書き込み権限
      id-token: write # <-- OIDC (認証トークン) の発行権限

    steps:
      # 1. リポジトリをクローン (LFSファイルを含めてダウンロード)
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true # LFSファイルを確実にダウンロード

      # 2. GitHub Pages の設定
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      # 3. WebGL ビルドファイルのアップロード
      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: . # リポジトリのルート (index.html, Build/など全て) を指定

      # 4. GitHub Pagesへのデプロイ
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4